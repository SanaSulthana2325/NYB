import {createContext} from 'react'

const ThemeContext = createContext();

export default ThemeContext

















// Context API:
// The Context API is a built-in React feature used for Global State Management. It allows you to share data between multiple components without passing props manually through every level of the component tree.


// Create Context: create a new file named userContext

// ↓

// Create Provider: this makes the data available to all child omponents

// ↓

// Wrap Components : every component inside app can access the context

// ↓

// Consume Context

// ↓

// Use Shared Data