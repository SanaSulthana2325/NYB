//DOM Traversing:( moving around your HTML family tree)

//1.ParentElement() 

let name=document.getElementById("name")
console.log(name)
console.log(name.parentElement)
// children 
let parentElement=name.parentElement
console.log(parentElement)
console.log(parentElement.children)
// firstElementChild
console.log(parentElement.firstElementChild) // shows first element
// lastElementChild
console.log(parentElement.lastElementChild) // shows last element
// sibling

// ex
let para = document.querySelector("p");
let parentDiv = para.parentElement;
console.log(parentDiv.id);
//
let button = document.querySelector("button");
let parentDiv1 = button.parentElement;
console.log(parentDiv1)
//
let div = document.querySelector("#container");
let allKids = div.children;
let firstKid = div.firstElementChild;
let lastKid = div.lastElementChild
console.log(allKids)
console.log(firstKid)
console.log(lastKid)
//siblingElement
let para2 = document.querySelector("p");
let nextBrother = para.nextElementSibling;
console.log(nextBrother)
let prevBrother = para.previousElementSibling;
console.log(prevBrother)

//ex2
let buyBtn = document.querySelector("#buyBtn");
console.log(buyBtn.parentElement.id);
//
let price = document.querySelector(".price");
console.log(price.nextElementSibling.id);
console.log(price.previousElementSibling.textContent);
//
let card = document.querySelector("#card")
console.log(card.children.length);
console.log(card.firstElementChild.className);

// creating a DOM element

let newElement=document.createElement('h1')
newElement.innerText = "New Element Created"
console.log(newElement)

let newElement1=document.createElement('h2')
newElement1.innerText=8885554442
console.log(newElement1)
parentElement.prepend(newElement1)


// append // means add at the end
parentElement.appendChild(newElement)
 //prepend// means add at the start or first

 parentElement.prepend(newElement)


 // before

 // after

 let btn=document.getElementById('btn')
 console.log(btn)
btn.onclick = function(){
    alert("Button Clicked")
    console.log("Button clicked")
}

// addEventlistener
btn.addEventListener("click",function() {
    alert("Button clicked")
})

 function click(){
   alert("Button clicked")
    btn.innerText = "Button clicked"
}
btn.addEventListener("click",click())


// ex for create a DOM element

let newDiv = document.createElement("div");
console.log(newDiv)
let newH3 = document.createElement("H3")
console.log(newH3)
let newP = document.createElement("P")
console.log(newP)
//
newH3.textContent = "I am Tasan made by Js"
//
newP.innerHTML = "<b>hii</b>";
document.body.append(newP);
//
newDiv.append(newH3);
newDiv.append(newP);
//
let parent = document.querySelector("#parent");
console.log(parent)
let target = document.querySelector("#target");
console.log(target)
//
parent.prepend(newDiv);
//
target.before("Text Before Target");
//
target.after("<i>Text After Target</i>");













