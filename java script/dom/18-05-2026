// call back function: it is function that5 is passed as an argument to another function and is executed later after a task is completed


function function1(s){
    console.log(s)
}

function one(a,b,c){
    console.log("one is executed")
    c(a+b)
}

one(1,2,function1)

// ex callback with parameters
function greet(name){
    console.log("hello  " +  name)
}
function processUser(callback) {
    let name="sana"
    callback(name)
}

processUser(greet)

//
function message(){
    console.log("callback function is executed")
}

function two(callback){
    callback()
}
two(message)

// addtion using callback
function display(results){
    console.log("results is:",results)
}
function add(e,f,callback){
    let sum = e + f
    callback(sum)
}
add(30, 90, display)

// Even or odd using callback
function checkResults(message){
    console.log(message)
}

function checkNumber(num, callback){
    if (num % 2 === 0){
        callback("Even Number")
    }
    else{
        callback("odd Number")
    }
}

checkNumber(4,checkResults)

// setTimeout callback

function showMessage(){
    console.log("executed after two seconds")
}
setTimeout(showMessage, 2000)

// anonymous callback function

function calculate(a, b, callback){
    callback(a,b)
}
calculate(5,8,function(x,y){
    console.log(x * y)
})

// array from one callback function

let number = [1,2,3,4,5]
number.forEach(function(num){
    console.log(num)
})

// callback Hell : multiple callback are nested  inside each other 

setTimeout(()=>{
    console.log("step-1")
    setTimeout(()=>{
        console.log("step-2")
        setTimeout(()=>{
            console.log("step-3")
        },1000)
    },1000)
},1000)


// using setInterval callback

function showMessage1(){
    console.log("executed after two seconds")
    console.log("interval running")
}
    
let intervalId = setInterval(showMessage1, 5000)

// to stop setInterval()


setTimeout(() =>{
    clearInterval(intervalId)
    console.log("Interval stopped")
},5000)

// substraction of callback
function display(results){
    console.log("results is:",results)
}
function sum(e,f,callback){
    let sum = e - f
    callback(sum)
}
sum(30, 90, display)


// promise: it is divided into 2 state like a pyramid
//1. pending : still working,result not ready
//2.resolve : for success and // it is used for .then(): waits until the promise is resolved and receives the results from the previous promise
//3.reject : for failure // it is use .catch(): if promise rejected or an error is occurs the control goes to  .catch()


let p = new Promise((resolve,reject) => {
    let status = true
    if(status){
        resolve("function success")
    } 
    else{
        reject("error")
    }
})

p
.then((a)=>{
    console.log(a)

})

.catch((b)=>{
    console.log(b)
})

//
let q = new Promise((resolve,reject) => {
    let status = false
    if(status){
        resolve("function success")
    } 
    else{
        reject("error")
    }
})

q
.then((a)=>{
    console.log(a)

})

.catch((b)=>{
    console.log(b)
})

// 1. pending promise 
let p1 = new Promise((resolve,reject) =>{
    console.log("promise is pending.......")
})

//2.resolve

let p2 = new Promise((resolve,reject) =>{
    let dataReady = true
    if(dataReady) {
        resolve("data fetched successfully")
    }
})
p2
.then((result) =>{
    console.log("success:", result)
})

//3. reject

let p3 = new Promise((resolve,reject)=>{
    let errorOccured = true
    if(errorOccured) {
        reject("Failed to fetch data")
    }
})

p3.catch((error) =>{
    console.log("error:", error)
})


//

let p4 = new Promise((resolve,reject) => {
    let status = false
    if(status){
        resolve("function  is success")
    } 
    else{
        reject("failed")
    }
})

p4
.then((a)=>{
    console.log(a)

})

.catch((b)=>{
    console.log(b)
})

// full execution
console.log("start")

let fetchData = new Promise((resolve,reject) =>{
    console.log("Inside promise: starting work....")
    let success = false

    setTimeout(() =>{
        if(success) {
            resolve("User data:{name: 'Sana'}")
        } else {
            reject("network error")
        }
    },2000)
})

console.log("after creating promise")

fetchData
.then((data) =>{
    console.log("resolved:", data)
})
.catch((err) =>{
    console.log("rejected",err)
})
.finally(() =>{
    console.log("promise settled")
})
 console.log("end")


 //Normal event: every typing function executes
 let button = document.getElementById("btn")
 button.addEventListener("click",function(){
    console.log("button clicked")
 })

 // throttling : event happens many times that allow function to run only onces in ine time

 //( runs functiona at fixed intervals)

 function throttle(function1, delay) {
    let lastCall = 0
    return function () {
        let currentTime = new Date().getTime();
        if (currentTime - lastCall >= delay){
            lastCall = currentTime;
            function1();
        }
    };
 }

 function showMessage(){
    console.log("scroll event executed");
 }
 window.addEventListener("scroll",throttle(showMessage, 2000)
);

// debouncing: waith until the user stop doing something and then execute the function
// like typing typing typing stop---> then executes onces

function debounce(func, delay) {
    let timer;
    return function(){
        clearTimeout(timer);
        timer = setTimeout(() => {
            func();
        }, delay);
    };
}
function searchData() {
    console.log("searching.....");
}
document.getElementById("search").addEventListener("keyup", debounce(searchData,2000)
);